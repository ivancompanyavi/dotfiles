# Same as /usr/share/xdg-desktop-portal/hyprland-portals.conf, except the
# Settings portal asks darkman first, so apps that follow the system light/dark
# preference (Firefox, GTK, WezTerm, Neovim) follow darkman.
[preferred]
default=hyprland;gtk
org.freedesktop.impl.portal.Settings=darkman;gtk
